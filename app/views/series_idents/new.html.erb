<h1>New series_ident</h1>

<% form_for(@series_ident) do |f| %>
  <%= f.error_messages %>

 <%= render :partial => 'entry', :locals	=> {:f => f} 	%>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', series_idents_path %>