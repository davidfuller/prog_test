<h1>New Sports IPP Media</h1>

<% form_for(@sports_ipp_media) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'entry', :locals	=> {:f => f } %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', sports_ipp_medias_path %>